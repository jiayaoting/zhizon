# 智子（Zhizon）AI 对话改造 · 运行验证清单

> 配套 doc/chat-ux-revamp-plan.md v1.4。编译通过后按此清单逐项验证；
> 每项记录「通过 / 失败 + 现象」，失败项贴回给 AI 修复。

## 0. 编译与安装
- [ ] DevEco Studio 打开项目，Sync + Build 通过（无 ERROR；WARN 可忽略）
- [ ] 真机 / 模拟器安装启动，无崩溃

## 1. 数据迁移（首次启动关键项）
- [ ] 旧版本（改造前）已有聊天记录：启动后自动生成一个「历史对话」会话，消息完整（含思考过程）
- [ ] 全新安装：无历史时自动创建「新的对话」会话
- [ ] 会话列表：固定（📌）置顶、其余按最近使用排序

## 2. 多会话管理
- [ ] 顶栏左侧 ☰ 打开会话抽屉
- [ ] 「＋ 新建对话」→ 新会话无消息，输入发送后标题保持「新的对话」
- [ ] 会话间切换：各会话消息互不串扰
- [ ] 长按会话项内 ✏️ 重命名（内联输入 + ✓ 确认）；📌 固定/取消固定；🗑️ 删除（弹确认）
- [ ] 删除当前会话后自动切换到最近会话或新建
- [ ] 清空会话（顶栏 🧹）只清当前会话消息，不删除会话
- [ ] 搜索框：输入关键字，标题命中与正文命中的会话都能出现（并集）

## 3. 对话体验
- [ ] 发送消息：连接中… → 生成中 Ns（计时）→ 完成后状态行消失
- [ ] 流式过程中 Markdown 直接渲染（标题/列表/表格/代码块实时排版，不跳变）
- [ ] 思考模式（DeepSeek/Claude）：💭 折叠块可展开/收起，思考与正文分离
- [ ] 代码块：头部有语言标注 + 「复制」按钮；带语言的代码块有语法高亮（ts/js/python/java/go/sql/bash 等）
- [ ] 任务列表 - [x] 渲染为 ☑
- [ ] 助手消息下方显示 ⚡ 输入/输出/共 Token（服务端返回 usage 时）
- [ ] 停止按钮：生成中点 ■ 停止，保留已生成内容，消息标记 stopped；无内容时移除空回复
- [ ] 网络错误：气泡显示 ⚠️ 请求失败 + 错误码 + 「↻ 重试」可重试

## 4. 消息操作
- [ ] 长按用户消息 → 「编辑」：内容载入输入框，发送后替换该条及其后所有消息（数据库同步）
- [ ] 长按助手消息 → 「重新生成」：替换本条回复，后续消息移除
- [ ] 复制/分享/多选/删除 功能正常（多选删除后列表刷新）
- [ ] 双击助手消息打开全屏 Markdown 预览（含思考过程）

## 5. 图片附件（vision 模型）
- [ ] 配置页开启「支持图片输入」的模型（如 OpenAI gpt-4o 系列 / Qwen-VL）
- [ ] 输入条出现「+」按钮 → 相册选图 → 显示图片名（可 ✕ 移除）
- [ ] 发送后模型能描述图片内容（OpenAI 格式 content 数组）
- [ ] Anthropic 格式（Claude 模型）同样可发图
- [ ] 无 vision 能力的模型不显示「+」按钮

## 6. 系统提示词
- [ ] 模型配置 → 高级配置 → 系统提示词填写后保存
- [ ] 对话时系统提示词生效（可让模型自我介绍验证）；清空后不注入

## 7. 导出 / 导入
- [ ] 抽屉底部「导出」：复制 Markdown 到剪贴板，内容含标题/角色/时间/思考引用块
- [ ] 「导入」：复制导出的 JSON（应用内导出需先有 JSON 渠道）→ 新会话恢复消息
- [ ] 导入非法内容：提示「格式无效」且不产生空会话

## 8. 回归（不应被改坏）
- [ ] 游戏中心各游戏可玩、AI 托管正常（LlmClient 非流式路径未动）
- [ ] AI 调用记录（我的页）正常记录/清空
- [ ] 主题切换/毛玻璃/字号档位在聊天页正常
- [ ] 手机/平板布局：抽屉宽度 78% / 320px 自适应
